import math
n = int(input('Digite um número para saber sua raiz: '))
raiz = math.sqrt(n)

# print(f'\n A raiz do {n} é {raiz:.1f}') - um print correto com apenas uma casa dedpois do ponto
# prnit(f'\n A raiz de {n} é {math.ceil(raiz)}, lembrando que todos os resultados são arredondados paa cima.') - um print sendo aredondado para cima, para baixo ficaria floor
